# apiVersion: skaffold/v4beta7
# kind: Config
# metadata:
#   name: cicd-gcp
# build:
#   artifacts:
#     - image: gcr.io/black-cirrus-461305-f6/cicd-gcp  # Replace with your image
#       docker:
#         dockerfile: app/Dockerfile  # Path to your Dockerfile
# deploy:
#   cloudrun:
#     services:
#       - name: dev  # Must match your target name in dev.yaml
#         projectId: black-cirrus-461305-f6
#         region: us-central1
#       - name: prod  # Must match your target name in prod.yaml
#         projectId: black-cirrus-461305-f6
#         region: us-central1
apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: cicd-gcp
build:
  artifacts:
  - image: gcr.io/black-cirrus-461305-f6/cicd-gcp
    docker:
      dockerfile: app/Dockerfile  # Path to your Dockerfile
deploy:
  kubectl:
    manifests:
      paths:
      # This empty section is required for Cloud Run deployments
      # even though we're not using kubectl directly
      # Cloud Deploy will override this with Cloud Run configuration
      # The empty kubectl section satisfies Skaffold's schema requirement
