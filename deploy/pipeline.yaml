# apiVersion: deploy.cloud.google.com/v1
# kind: DeliveryPipeline
# metadata:
#   name: my-pipeline
#   labels:
#     app: cicd
# description: cicd delivery pipeline
# serialPipeline:
#   stages:
#   - targetId: dev
#   - targetId: prod

# apiVersion: deploy.cloud.google.com/v1
# kind: DeliveryPipeline
# metadata:
#   name: my-pipeline
#   labels:
#     app: cicd
# description: cicd delivery pipeline
# serialPipeline:
#   stages:
#   - targetId: dev
#   - targetId: prod
apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: my-pipeline
description: Delivery pipeline for YOUR_APP_NAME
serialPipeline:
  stages:
  - targetId: development
    profiles: ["dev"]
  - targetId: production
    profiles: ["prod"]
