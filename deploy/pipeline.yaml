# apiVersion: deploy.cloud.google.com/v1
# kind: DeliveryPipeline
# metadata:
#   name: my-pipeline
#   labels:
#     app: cicd
# description: cicd delivery pipeline
# serialPipeline:
#   stages:
#   - targetId: dev
#   - targetId: prod

apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: my-pipeline
  labels:
    app: cicd
description: cicd delivery pipeline
serialPipeline:
  stages:
  - targetId: dev
    profiles: ["cloudrun"]  # Add this profile
  - targetId: prod
    profiles: ["cloudrun"]  # Add this profile
